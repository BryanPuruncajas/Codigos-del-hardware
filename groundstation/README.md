# Ground station

1. Ajusta `user_parameters.py`.
2. `pip install -r requirements.txt`
3. Empieza con `python run_test.py p01 --seconds 30`.

Pruebas con movimiento exigen escribir `ARMAR`. Para P03/P05 usa `--yaw-deg`; para P04/P05 usa `--height`.
